## 🚀 How to Run Locally

### 1️⃣ Start the Collector Server

Open your terminal and run:

```bash
python test.py
```

**Description:**

- Starts the UDP server listening on port `5555`.
- Logs all incoming packets to:
  - `telemetry_log.csv`
